---
title: Sıralama
description: Sütun başlıklarına tıklayarak tablo verilerinizi sıralayın
---

import { LuxTableSortingDemo } from "../../components/lux-table-demo";

# Sıralama

LuxTable yerleşik sıralama işlevselliği sağlar. Sıralama seçeneklerine erişmek için sütun başlıklarına tıklamanız yeterli.

## Canlı Demo

<LuxTableSortingDemo />

## Temel Kullanım

Tek bir prop ile sıralamayı etkinleştirin:

```tsx
<LuxTable 
  data={data} 
  columns={columns} 
  options={{ sorting: true }}
/>
```

Sıralama etkinleştirildiğinde tüm sütunlar varsayılan olarak sıralanabilir.

---

## Belirli Sütunlarda Sıralamayı Devre Dışı Bırakma

Belirli sütunlarda sıralamayı devre dışı bırakabilirsiniz:

```tsx
const columns = [
  columnHelper.accessor("isim", { 
    header: "İsim",
    enableSorting: true,  // Varsayılan: true
  }),
  
  columnHelper.accessor("islemler", {
    header: "İşlemler",
    enableSorting: false, // 👈 Bu sütun için sıralama yok
  }),
];
```

---

## Kontrollü Sıralama

İleri düzey kullanım durumları için sıralama durumunu dışarıdan kontrol edebilirsiniz:

```tsx
import { useState } from "react";
import { LuxTable, SortingState } from "luxtable";

export default function App() {
  const [sorting, setSorting] = useState<SortingState>([]);

  return (
    <LuxTable 
      data={data} 
      columns={columns} 
      sorting={sorting}
      onSortingChange={setSorting}
      options={{ sorting: true }} 
    />
  );
}
```

### SortingState Formatı

Sıralama durumu, sıralama nesnelerinden oluşan bir dizidir:

```tsx
// Tek sütun sıralaması
const sorting = [{ id: "isim", desc: false }]; // İsme göre artan

// Çoklu sütun sıralaması (etkinleştirilmişse)
const sorting = [
  { id: "departman", desc: false },
  { id: "isim", desc: true },
];
```

---

## Varsayılan Sıralama

Varsayılan bir sıralama durumu sağlayarak başlangıç sıralaması ayarlayabilirsiniz:

```tsx
import { useState } from "react";
import { LuxTable, SortingState } from "luxtable";

export default function App() {
  // İsim sütunu artan sıralı başla
  const [sorting, setSorting] = useState<SortingState>([
    { id: "isim", desc: false }
  ]);

  return (
    <LuxTable 
      data={data} 
      columns={columns} 
      sorting={sorting}
      onSortingChange={setSorting}
      options={{ sorting: true }} 
    />
  );
}
```

---

## Sıralama Göstergeleri ve Aksiyonlar

### Hızlı Sıralama
Sıralamayı değiştirmek için doğrudan sütun başlığına tıklayın:
- **İlk tıklama**: Artan sıralama (↑)
- **İkinci tıklama**: Azalan sıralama (↓)
- **Üçüncü tıklama**: Tekrar artan

### Sütun Aksiyonları Menüsü
Aksiyon menüsünü (⋮) görmek için sütun başlığının üzerine gelin. Menü şunları içerir:
- **Artan Sırala** - A→Z veya 0→9 sırala
- **Azalan Sırala** - Z→A veya 9→0 sırala
- **Sıralamayı temizle** - Sıralamayı kaldır (yalnızca sıralandığında)
- **Sütunu gizle** - Sütunu görünümden kaldır

### Görsel Göstergeler
- **↑** (Yukarı Ok) - Şu anda artan sıralı
- **↓** (Aşağı Ok) - Şu anda azalan sıralı
- **⇅** (Çift Ok) - Sıralanabilir ama şu anda sıralı değil

